<!DOCTYPE html>
<head>
  <link type="text/css" href="app\assets\stylesheets\homepage.css.scss" rel="stylesheet" />
  <link href='http://fonts.googleapis.com/css?family=Montaga' rel='stylesheet' type='text/css'>
  <link href='http://fonts.googleapis.com/css?family=Fenix' rel='stylesheet' type='text/css'>
</head>

<body>
<h1>Listing Top Ipad games</h1>
 
 <div class="games_table">
    <table>
      <tr>
        <th>Rank</th>
        <th>Title</th>
        <th>Downloads</th>
        <th>Price</th>
        <th>Comments</th>
      </tr>
 
      <% @ipad.each do |i| %>
      <tr>
          <td><%= i.rank %></td>
          <td><%= i.title %></td>
          <td><%= i.downloads %></td>
          <td><%= i.price %></td>
          <td><%= i.comments %></td>
      </tr>
      <% end %>
    </table>

    <div class="license">
      <h6>All rigths reserved to Anton Romanov(c)</h6>
        <h3>
        <%= link_to "Back to Homepage!", home_path, class: "btn-custom btn-small btn-primary" %>
        <%= link_to "Back to WebParser for Ipad games", ipadgames_path, class: "btn-custom btn-small btn-primary" %>
        </h3>
    </div>

  </div>

</body>
